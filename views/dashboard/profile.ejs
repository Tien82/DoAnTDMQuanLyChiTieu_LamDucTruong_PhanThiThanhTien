<%- include('../partials/header') %>

<div class="container mt-4">
    <h3 class="fw-bold mb-4"><i class="bi bi-person-gear me-2"></i>Cài đặt tài khoản</h3>

    <div class="row g-4">
        <div class="col-md-6">
            <div class="card border-0 shadow-sm p-4 h-100" style="border-radius: 20px;">
                <div class="text-center mb-4">
                    <div class="position-relative d-inline-block">
                        <img src="https://ui-avatars.com/api/?name=<%= user %>&background=0D6EFD&color=fff&size=128" 
                             class="rounded-circle shadow-sm" alt="avatar">
                        <span class="position-absolute bottom-0 end-0 badge rounded-pill bg-primary border border-2 border-white p-2">
                            <i class="bi bi-camera-fill"></i>
                        </span>
                    </div>
                    <h5 class="fw-bold mt-3 mb-0"><%= user %></h5>
                    <p class="text-muted small">Thành viên từ 2024</p>
                </div>

                <form action="/dashboard/profile/update" method="POST">
                    <div class="mb-3">
                        <label class="form-label small fw-bold text-muted">HỌ VÀ TÊN</label>
                        <input type="text" name="hoVaTen" class="form-control border-0 bg-light py-2" value="<%= user %>">
                    </div>
                    <div class="mb-3">
                        <label class="form-label small fw-bold text-muted">TÊN ĐĂNG NHẬP</label>
                        <input type="text" class="form-control border-0 bg-light py-2" value="user_demo" disabled>
                        <small class="text-info" style="font-size: 0.7rem;">Tên đăng nhập không thể thay đổi</small>
                    </div>
                    <button type="submit" class="btn btn-primary w-100 py-2 fw-bold rounded-pill mt-3">Cập nhật hồ sơ</button>
                </form>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card border-0 shadow-sm p-4 mb-4" style="border-radius: 20px;">
                <h6 class="fw-bold mb-3"><i class="bi bi-piggy-bank text-success me-2"></i>Quản lý hạn mức</h6>
                <p class="text-muted small">Thiết lập số tiền tối đa ný muốn tiêu trong một tháng để hệ thống AI cảnh báo kịp thời.</p>
                
                <form action="/dashboard/profile/budget" method="POST">
                    <div class="mb-4">
                        <label class="form-label small fw-bold text-muted">HẠN MỨC THÁNG (VNĐ)</label>
                        <div class="input-group">
                            <input type="number" name="hanMucThang" class="form-control border-0 bg-light py-2" value="5000000">
                            <span class="input-group-text border-0 bg-light fw-bold">VNĐ</span>
                        </div>
                    </div>
                    <button type="submit" class="btn btn-success w-100 py-2 fw-bold rounded-pill">Lưu hạn mức mới</button>
                </form>
            </div>

            <div class="card border-0 shadow-sm p-4" style="border-radius: 20px;">
                <h6 class="fw-bold mb-3"><i class="bi bi-shield-lock text-warning me-2"></i>Bảo mật</h6>
                <button class="btn btn-outline-secondary w-100 py-2 fw-bold rounded-pill mb-2">Đổi mật khẩu</button>
                <button class="btn btn-outline-danger w-100 py-2 fw-bold rounded-pill">Xóa tài khoản</button>
            </div>
        </div>
    </div>
</div>

<%- include('../partials/footer') %>